#include <gtest/gtest.h>
#include "Actor/actor.h"

TEST(ActorTest, creation)
{
//  Actor actor("player", '@', 10,20);

//  EXPECT_EQ(actor.getChar(), '@');
//  EXPECT_EQ(actor.getName(), "Player");
//  EXPECT_EQ(actor.getX(), 10);
//  EXPECT_EQ(actor.getY(), 20);
}
